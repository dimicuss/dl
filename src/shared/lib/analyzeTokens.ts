import {Token, Tokens} from "../types/editor";

function createChecker(tokenSeq: Tokens[]) {
  return function check() {

  }
}

export function analyzeTokens(tokens: Token[]) {
  if (tokens.length) {
    const token = tokens[0]

  }
}
